# Copy to `.deploy.env` (gitignored) and fill in, then run:
#   set -a; source .deploy.env; set +a; ./deploy.sh
#
# Never commit real passwords. .deploy.env is ignored by git.

SSH_HOST=103.192.198.87
SSH_PORT=22
SSH_USER=intelhunt-demos
SSH_PASSWORD=            # the Site User password for intelhunt-demos
APP_DIR=/home/intelhunt-demos/htdocs/demos.intelhunt.com/Druva-Sports
BASE_PATH=/druva-sports/
VITE_SITE_URL=https://demos.intelhunt.com/druva-sports
